{
  "name": "stt-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal local speech-to-text dictation MVP (Typeless-like) — Electron + transformers.js, zero native modules.",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && electron .",
    "test:stt": "npm run build && electron . --selftest"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
